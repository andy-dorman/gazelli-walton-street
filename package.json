{
  "name": "gazellis-keys-pledge",
  "version": "0.0.0",
  "description": "Promotional website for the Walton Street Launch",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.3.11",
    "grunt": "^0.4.5",
    "grunt-contrib-compass": "^1.0.1",
    "grunt-contrib-jshint": "^0.10.0",
    "grunt-contrib-uglify": "^0.6.0",
    "grunt-contrib-watch": "^0.6.1"
  },
  "scripts": {
    "postinstall": "bower install --force-latest",
    "prestart": "npm install",
    "pretest": "npm install"
  }
}
